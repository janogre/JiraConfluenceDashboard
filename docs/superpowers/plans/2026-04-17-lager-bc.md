# Lager – Business Central integrasjon – Implementasjonsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Legg til et nytt «Lager»-menyvalg som henter og viser lagerdata for telekom-relevante varer fra Microsoft Business Central via en server-side proxy-modul.

**Architecture:** En Express-router i `server/businessCentral/` håndterer all BC-kommunikasjon (token-cache, paginering) og eksponerer `GET /api/bc/items`. Frontend-siden `src/pages/Lager/Lager.tsx` bruker TanStack Query og filtrerer/søker client-side.

**Tech Stack:** Node.js / Express (server), React 19 / TypeScript / TanStack Query / CSS Modules (frontend), Microsoft Business Central OData API v2.0, Entra ID OAuth 2.0 client credentials.

---

## Filstruktur

| Fil | Handling | Ansvar |
|---|---|---|
| `.env` | Opprett | BC-miljøvariabler (aldri commit) |
| `.env.example` | Opprett | Mal uten secret |
| `.gitignore` | Modifiser | Legg til `.env` |
| `server/businessCentral/auth.js` | Opprett | Token-henting og in-memory cache |
| `server/businessCentral/itemsService.js` | Opprett | OData-kall med paginering, 401-retry |
| `server/businessCentral/index.js` | Opprett | Express router, feilhåndtering |
| `server/proxy.js` | Modifiser | Importer og monter BC-router |
| `src/types/index.ts` | Modifiser | Legg til `BcItem`-interface |
| `src/services/bcService.ts` | Opprett | Kall `/api/bc/items`, returner `BcItemsResponse` |
| `src/pages/Lager/Lager.tsx` | Opprett | Lagersiden med tabell, søk og filtre |
| `src/pages/Lager/Lager.module.css` | Opprett | Styling for lagersiden |
| `src/App.tsx` | Modifiser | Ny route `/lager` |
| `src/components/Layout/LayoutV2.tsx` | Modifiser | Nytt nav-innslag «Lager» |

---

## Task 1: Miljøvariabler og .gitignore

**Files:**
- Opprett: `.env`
- Opprett: `.env.example`
- Modifiser: `.gitignore`

- [ ] **Steg 1: Legg `.env` til .gitignore**

Åpne `.gitignore` og legg til disse linjene etter den eksisterende `server/.env`-linjen:

```
.env
```

- [ ] **Steg 2: Opprett `.env` i prosjektrot**

Opprett filen `.env` med faktisk secret (fyll inn `BC_CLIENT_SECRET`):

```
# Atlassian (legg til eksisterende verdier hvis de finnes)
# ATLASSIAN_CLIENT_ID=...
# ATLASSIAN_CLIENT_SECRET=...

# Microsoft Business Central
BC_TENANT_ID=25138f26-f059-4e5b-a9af-095e3f965684
BC_CLIENT_ID=9e72f2d0-d0fb-4828-896f-b61ce518038f
BC_CLIENT_SECRET=<din-secret-her>
BC_ENVIRONMENT=Production
BC_COMPANY_ID=4a3dede8-1019-eb11-bf6a-000d3ab0b154
BC_ITEM_GROUPS=KOM,DRIFT
```

- [ ] **Steg 3: Opprett `.env.example`**

```
# Atlassian
# ATLASSIAN_CLIENT_ID=
# ATLASSIAN_CLIENT_SECRET=

# Microsoft Business Central
BC_TENANT_ID=
BC_CLIENT_ID=
BC_CLIENT_SECRET=<secret-fra-Entra-ID>
BC_ENVIRONMENT=Production
BC_COMPANY_ID=
BC_ITEM_GROUPS=KOM,DRIFT
```

- [ ] **Steg 4: Verifiser at `.env` er ignorert**

```bash
git status
```

Forventet: `.env` skal IKKE dukke opp som untracked fil. Hvis den gjør det, dobbeltsjekk `.gitignore`.

- [ ] **Steg 5: Commit**

```bash
git add .env.example .gitignore
git commit -m "chore: legg til .env.example og .env i .gitignore for BC-integrasjon"
```

---

## Task 2: Token-cache (`server/businessCentral/auth.js`)

**Files:**
- Opprett: `server/businessCentral/auth.js`

- [ ] **Steg 1: Opprett katalogen og filen**

```bash
mkdir -p server/businessCentral
```

Opprett `server/businessCentral/auth.js`:

```js
let cache = { token: null, expiresAt: 0 };

export async function getBcToken() {
  if (cache.token && Date.now() < cache.expiresAt) {
    console.log('[BC auth] Cache-treff – gjenbruker token');
    return cache.token;
  }

  console.log('[BC auth] Henter nytt token fra Entra ID');
  const params = new URLSearchParams({
    grant_type: 'client_credentials',
    client_id: process.env.BC_CLIENT_ID,
    client_secret: process.env.BC_CLIENT_SECRET,
    scope: 'https://api.businesscentral.dynamics.com/.default',
  });

  const resp = await fetch(
    `https://login.microsoftonline.com/${process.env.BC_TENANT_ID}/oauth2/v2.0/token`,
    {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: params,
    }
  );

  if (!resp.ok) {
    const text = await resp.text();
    console.error('[BC auth] Token-henting feilet:', resp.status, text);
    const err = new Error(`BC token-henting feilet (${resp.status})`);
    err.status = 401;
    throw err;
  }

  const data = await resp.json();
  cache.token = data.access_token;
  cache.expiresAt = Date.now() + (data.expires_in - 60) * 1000;
  console.log(`[BC auth] Nytt token cachet, gyldig i ${data.expires_in}s`);
  return cache.token;
}

export function invalidateBcTokenCache() {
  cache = { token: null, expiresAt: 0 };
  console.log('[BC auth] Cache invalidert');
}
```

- [ ] **Steg 2: Manuell smoke-test av auth-modulen**

Start proxy-serveren og sjekk at den starter uten feil:

```bash
npm run proxy
```

Forventet output: `Atlassian API Proxy Server` starter på port 3001 uten stack trace.

- [ ] **Steg 3: Commit**

```bash
git add server/businessCentral/auth.js
git commit -m "feat: legg til BC token-cache i server/businessCentral/auth.js"
```

---

## Task 3: Items-service med paginering (`server/businessCentral/itemsService.js`)

**Files:**
- Opprett: `server/businessCentral/itemsService.js`

- [ ] **Steg 1: Opprett `server/businessCentral/itemsService.js`**

```js
import { getBcToken, invalidateBcTokenCache } from './auth.js';

function buildOdataFilter() {
  const groups = (process.env.BC_ITEM_GROUPS || 'KOM,DRIFT')
    .split(',')
    .map((g) => g.trim())
    .filter(Boolean);
  return groups.map((g) => `inventoryPostingGroupCode eq '${g}'`).join(' or ');
}

async function fetchAllPages(token) {
  const filter = buildOdataFilter();
  const select = 'number,displayName,displayName2,inventory,inventoryPostingGroupCode,lastModifiedDateTime';
  const base = `https://api.businesscentral.dynamics.com/v2.0/${process.env.BC_TENANT_ID}/${process.env.BC_ENVIRONMENT}/api/v2.0`;
  let url =
    `${base}/companies(${process.env.BC_COMPANY_ID})/items` +
    `?$filter=${encodeURIComponent(filter)}&$select=${select}&$top=1000`;

  const items = [];
  let pageCount = 0;

  while (url) {
    pageCount++;
    console.log(`[BC items] Henter side ${pageCount}: ${url.substring(0, 120)}…`);

    const resp = await fetch(url, {
      headers: { Authorization: `Bearer ${token}`, Accept: 'application/json' },
    });

    if (!resp.ok) {
      const body = await resp.text();
      console.error(`[BC items] API-feil ${resp.status}:`, body.substring(0, 300));
      const err = new Error(`BC API feilet (${resp.status})`);
      err.status = resp.status;
      err.body = body;
      throw err;
    }

    const data = await resp.json();
    items.push(...(data.value ?? []));
    url = data['@odata.nextLink'] ?? null;
  }

  console.log(`[BC items] Totalt ${items.length} varer hentet over ${pageCount} side(r)`);
  return items;
}

export async function getBcItems() {
  let token = await getBcToken();
  try {
    return await fetchAllPages(token);
  } catch (err) {
    if (err.status === 401) {
      console.log('[BC items] 401 mottatt – invaliderer cache og prøver på nytt');
      invalidateBcTokenCache();
      token = await getBcToken();
      return await fetchAllPages(token);
    }
    throw err;
  }
}
```

- [ ] **Steg 2: Commit**

```bash
git add server/businessCentral/itemsService.js
git commit -m "feat: legg til BC items-service med OData-paginering"
```

---

## Task 4: Express-router (`server/businessCentral/index.js`)

**Files:**
- Opprett: `server/businessCentral/index.js`

- [ ] **Steg 1: Opprett `server/businessCentral/index.js`**

```js
import { Router } from 'express';
import { getBcItems } from './itemsService.js';

const router = Router();

router.get('/items', async (req, res) => {
  const start = Date.now();
  try {
    const items = await getBcItems();
    const elapsed = Date.now() - start;
    console.log(`[BC router] /items → ${items.length} varer, ${elapsed}ms`);
    res.json({ items, fetchedAt: new Date().toISOString() });
  } catch (err) {
    console.error('[BC router] Feil:', err.message);

    if (err.status === 401) {
      return res.status(401).json({ error: 'BC-autentisering feilet. Kontakt administrator – sjekk BC_CLIENT_SECRET i .env.' });
    }
    if (err.code === 'ECONNREFUSED' || err.code === 'ENOTFOUND' || err.code === 'ETIMEDOUT') {
      return res.status(503).json({ error: 'Kunne ikke nå Business Central. Sjekk nettverkstilkobling og prøv igjen.' });
    }
    res.status(500).json({
      error: `Business Central returnerte en feil (HTTP ${err.status ?? 500}).`,
      detail: err.message,
    });
  }
});

export default router;
```

- [ ] **Steg 2: Commit**

```bash
git add server/businessCentral/index.js
git commit -m "feat: legg til BC Express-router på /api/bc/items"
```

---

## Task 5: Monter BC-router i proxy.js

**Files:**
- Modifiser: `server/proxy.js`

- [ ] **Steg 1: Legg til import øverst i `server/proxy.js`**

Etter den eksisterende `import`-blokken (etter linje 7 der `FileStore` defineres), legg til:

```js
import bcRouter from './businessCentral/index.js';
```

- [ ] **Steg 2: Monter routeren**

Finn linjen `app.use(express.json());` (ca. linje 19) og legg til monteringen rett etter:

```js
app.use('/api/bc', bcRouter);
```

- [ ] **Steg 3: Start proxy og test endepunktet manuelt**

```bash
npm run proxy
```

I et nytt terminalvindu:

```bash
curl http://localhost:3001/api/bc/items
```

Forventet: JSON-objekt med `{ "items": [...], "fetchedAt": "..." }`. Konsollutput skal vise `[BC auth] Henter nytt token fra Entra ID` første gang, deretter `[BC auth] Cache-treff` ved neste kall.

- [ ] **Steg 4: Commit**

```bash
git add server/proxy.js
git commit -m "feat: monter BC-router på /api/bc i proxy.js"
```

---

## Task 6: TypeScript-type og frontend-service

**Files:**
- Modifiser: `src/types/index.ts`
- Opprett: `src/services/bcService.ts`

- [ ] **Steg 1: Legg til `BcItem`-interface i `src/types/index.ts`**

Legg til disse linjene på slutten av filen:

```ts
// Business Central – Lager
export interface BcItem {
  number: string;
  displayName: string;
  displayName2: string;
  inventory: number;
  inventoryPostingGroupCode: string;
  lastModifiedDateTime: string;
}

export interface BcItemsResponse {
  items: BcItem[];
  fetchedAt: string;
}
```

- [ ] **Steg 2: Opprett `src/services/bcService.ts`**

```ts
import axios from 'axios';
import type { BcItemsResponse } from '../types';

export async function fetchBcItems(): Promise<BcItemsResponse> {
  const resp = await axios.get<BcItemsResponse>('/api/bc/items', {
    withCredentials: true,
  });
  return resp.data;
}
```

- [ ] **Steg 3: Verifiser TypeScript-kompilering**

```bash
npm run build
```

Forventet: Ingen TypeScript-feil.

- [ ] **Steg 4: Commit**

```bash
git add src/types/index.ts src/services/bcService.ts
git commit -m "feat: legg til BcItem-typer og bcService"
```

---

## Task 7: Lagersiden (`src/pages/Lager/`)

**Files:**
- Opprett: `src/pages/Lager/Lager.tsx`
- Opprett: `src/pages/Lager/Lager.module.css`

- [ ] **Steg 1: Opprett `src/pages/Lager/Lager.module.css`**

```css
.container {
  padding: 24px;
  max-width: 1400px;
}

.toolbar {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 0;
  flex-wrap: wrap;
}

.searchWrapper {
  position: relative;
  flex: 1;
  min-width: 200px;
}

.searchIcon {
  position: absolute;
  left: 10px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--color-text-secondary);
  pointer-events: none;
}

.searchInput {
  width: 100%;
  padding: 8px 12px 8px 34px;
  border: 1px solid var(--color-border);
  border-radius: 6px;
  background: var(--color-bg);
  color: var(--color-text);
  font-size: 14px;
  box-sizing: border-box;
}

.searchInput:focus {
  outline: none;
  border-color: var(--color-accent);
}

.select {
  padding: 8px 12px;
  border: 1px solid var(--color-border);
  border-radius: 6px;
  background: var(--color-bg);
  color: var(--color-text);
  font-size: 14px;
  cursor: pointer;
}

.toggleLabel {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 13px;
  color: var(--color-text-secondary);
  white-space: nowrap;
  cursor: pointer;
  user-select: none;
}

.toggle {
  width: 36px;
  height: 20px;
  background: var(--color-border);
  border-radius: 10px;
  position: relative;
  transition: background 0.2s;
  flex-shrink: 0;
}

.toggle::after {
  content: '';
  position: absolute;
  width: 16px;
  height: 16px;
  background: #fff;
  border-radius: 50%;
  top: 2px;
  left: 2px;
  transition: left 0.2s;
}

.toggleActive {
  background: #22c55e;
}

.toggleActive::after {
  left: 18px;
}

.refreshBtn {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 14px;
  border: 1px solid var(--color-border);
  border-radius: 6px;
  background: var(--color-bg);
  color: var(--color-text);
  font-size: 14px;
  cursor: pointer;
  white-space: nowrap;
  transition: background 0.15s;
}

.refreshBtn:hover {
  background: var(--color-bg-secondary);
}

.refreshBtn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.statusBar {
  padding: 6px 0;
  font-size: 12px;
  color: var(--color-text-secondary);
  display: flex;
  justify-content: space-between;
  border-bottom: 1px solid var(--color-border);
  margin-bottom: 0;
}

.tableWrapper {
  overflow-x: auto;
}

.table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}

.table th {
  text-align: left;
  padding: 10px 12px;
  font-size: 11px;
  font-weight: 600;
  color: var(--color-text-secondary);
  border-bottom: 2px solid var(--color-border);
  white-space: nowrap;
}

.table th.sortable {
  cursor: pointer;
}

.table th.sortable:hover {
  color: var(--color-text);
}

.table td {
  padding: 9px 12px;
  border-bottom: 1px solid var(--color-border);
  vertical-align: middle;
}

.row:hover td {
  background: var(--color-bg-secondary);
}

.rowEmpty td {
  opacity: 0.45;
}

.varenr {
  font-family: monospace;
  font-size: 12px;
  color: var(--color-text-secondary);
}

.desc2 {
  font-size: 12px;
  color: var(--color-text-secondary);
}

.groupBadge {
  display: inline-block;
  padding: 2px 8px;
  border: 1px solid var(--color-border);
  border-radius: 4px;
  font-size: 11px;
  background: var(--color-bg-secondary);
}

.inventory {
  text-align: right;
  font-weight: 700;
  font-size: 15px;
}

.inventoryGreen  { color: #22c55e; }
.inventoryOrange { color: #f59e0b; }
.inventoryRed    { color: #ef4444; }

.dateCell {
  font-size: 12px;
  color: var(--color-text-secondary);
  white-space: nowrap;
}

.error {
  margin: 24px 0;
  padding: 16px 20px;
  border-radius: 8px;
  border: 1px solid #f59e0b44;
  background: #f59e0b11;
}

.errorTitle {
  font-weight: 600;
  color: #f59e0b;
  margin-bottom: 6px;
}

.errorText {
  font-size: 13px;
  color: var(--color-text-secondary);
  margin-bottom: 12px;
}

.retryBtn {
  padding: 6px 14px;
  border: 1px solid var(--color-border);
  border-radius: 6px;
  background: var(--color-bg);
  color: var(--color-text);
  font-size: 13px;
  cursor: pointer;
}

.loading {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 60px;
  color: var(--color-text-secondary);
  font-size: 14px;
  gap: 10px;
}
```

- [ ] **Steg 2: Opprett `src/pages/Lager/Lager.tsx`**

```tsx
import { useState, useMemo } from 'react';
import { useQuery } from '@tanstack/react-query';
import { Search, RefreshCw, Package } from 'lucide-react';
import { fetchBcItems } from '../../services/bcService';
import type { BcItem } from '../../types';
import styles from './Lager.module.css';

type SortField = 'number' | 'displayName' | 'inventory';
type SortDir = 'asc' | 'desc';

function formatDate(iso: string): string {
  return new Date(iso).toLocaleDateString('nb-NO', { day: 'numeric', month: 'short', year: 'numeric' });
}

function inventoryClass(n: number): string {
  if (n >= 10) return styles.inventoryGreen;
  if (n >= 1)  return styles.inventoryOrange;
  return styles.inventoryRed;
}

function sortIcon(field: SortField, current: SortField, dir: SortDir): string {
  if (field !== current) return ' ↕';
  return dir === 'asc' ? ' ↑' : ' ↓';
}

export function Lager() {
  const [search, setSearch]         = useState('');
  const [group, setGroup]           = useState('');
  const [hideEmpty, setHideEmpty]   = useState(false);
  const [sortField, setSortField]   = useState<SortField>('number');
  const [sortDir, setSortDir]       = useState<SortDir>('asc');

  const { data, isLoading, isError, error, refetch, isFetching, dataUpdatedAt } = useQuery({
    queryKey: ['bc-items'],
    queryFn: fetchBcItems,
    staleTime: 1000 * 60 * 5,
  });

  const allGroups = useMemo(() => {
    if (!data) return [];
    return [...new Set(data.items.map((i) => i.inventoryPostingGroupCode))].sort();
  }, [data]);

  const filtered = useMemo(() => {
    if (!data) return [];
    const q = search.toLowerCase();
    return data.items.filter((item) => {
      if (hideEmpty && item.inventory === 0) return false;
      if (group && item.inventoryPostingGroupCode !== group) return false;
      if (q && !item.number.toLowerCase().includes(q) && !item.displayName.toLowerCase().includes(q)) return false;
      return true;
    });
  }, [data, search, group, hideEmpty]);

  const sorted = useMemo(() => {
    return [...filtered].sort((a, b) => {
      let cmp = 0;
      if (sortField === 'number')      cmp = a.number.localeCompare(b.number);
      if (sortField === 'displayName') cmp = a.displayName.localeCompare(b.displayName);
      if (sortField === 'inventory')   cmp = a.inventory - b.inventory;
      return sortDir === 'asc' ? cmp : -cmp;
    });
  }, [filtered, sortField, sortDir]);

  function toggleSort(field: SortField) {
    if (sortField === field) {
      setSortDir((d) => (d === 'asc' ? 'desc' : 'asc'));
    } else {
      setSortField(field);
      setSortDir('asc');
    }
  }

  const fetchedAt = dataUpdatedAt
    ? new Date(dataUpdatedAt).toLocaleTimeString('nb-NO', { hour: '2-digit', minute: '2-digit' })
    : null;

  const errorMessage = (() => {
    if (!isError || !error) return null;
    const msg = (error as { response?: { data?: { error?: string } }; message?: string })
      ?.response?.data?.error ?? (error as Error).message;
    if (msg?.includes('autentisering')) return msg;
    if (msg?.includes('Business Central')) return msg;
    return 'Kunne ikke nå Business Central. Sjekk nettverkstilkobling og prøv igjen.';
  })();

  return (
    <div className={styles.container}>
      {/* Toolbar */}
      <div className={styles.toolbar}>
        <div className={styles.searchWrapper}>
          <Search size={15} className={styles.searchIcon} />
          <input
            className={styles.searchInput}
            placeholder="Søk varenr / navn…"
            value={search}
            onChange={(e) => setSearch(e.target.value)}
          />
        </div>

        <select
          className={styles.select}
          value={group}
          onChange={(e) => setGroup(e.target.value)}
        >
          <option value="">Alle grupper</option>
          {allGroups.map((g) => (
            <option key={g} value={g}>{g}</option>
          ))}
        </select>

        <label className={styles.toggleLabel}>
          <div
            className={`${styles.toggle} ${hideEmpty ? styles.toggleActive : ''}`}
            onClick={() => setHideEmpty((v) => !v)}
          />
          Skjul tomt lager
        </label>

        <button
          className={styles.refreshBtn}
          onClick={() => refetch()}
          disabled={isFetching}
        >
          <RefreshCw size={14} />
          {isFetching ? 'Henter…' : 'Oppdater'}
        </button>
      </div>

      {/* Statuslinje */}
      {data && (
        <div className={styles.statusBar}>
          <span>
            Viser {sorted.length} av {data.items.length} varer
            {hideEmpty && ` (${data.items.filter(i => i.inventory === 0).length} med lager = 0 skjult)`}
          </span>
          {fetchedAt && <span>Hentet kl. {fetchedAt}</span>}
        </div>
      )}

      {/* Feilmelding */}
      {isError && (
        <div className={styles.error}>
          <div className={styles.errorTitle}>⚠ Feil ved henting av lagerdata</div>
          <div className={styles.errorText}>{errorMessage}</div>
          <button className={styles.retryBtn} onClick={() => refetch()}>↻ Prøv igjen</button>
        </div>
      )}

      {/* Laster */}
      {isLoading && (
        <div className={styles.loading}>
          <Package size={20} />
          Henter lagerdata fra Business Central…
        </div>
      )}

      {/* Tabell */}
      {data && !isError && (
        <div className={styles.tableWrapper}>
          <table className={styles.table}>
            <thead>
              <tr>
                <th className={styles.sortable} onClick={() => toggleSort('number')}>
                  VARENR{sortIcon('number', sortField, sortDir)}
                </th>
                <th className={styles.sortable} onClick={() => toggleSort('displayName')}>
                  NAVN{sortIcon('displayName', sortField, sortDir)}
                </th>
                <th>BESKRIVELSE 2</th>
                <th>GRUPPE</th>
                <th
                  className={styles.sortable}
                  style={{ textAlign: 'right' }}
                  onClick={() => toggleSort('inventory')}
                >
                  LAGER{sortIcon('inventory', sortField, sortDir)}
                </th>
                <th>OPPDATERT</th>
              </tr>
            </thead>
            <tbody>
              {sorted.map((item: BcItem) => (
                <tr
                  key={item.number}
                  className={`${styles.row} ${item.inventory === 0 ? styles.rowEmpty : ''}`}
                >
                  <td className={styles.varenr}>{item.number}</td>
                  <td>{item.displayName}</td>
                  <td className={styles.desc2}>{item.displayName2}</td>
                  <td><span className={styles.groupBadge}>{item.inventoryPostingGroupCode}</span></td>
                  <td className={`${styles.inventory} ${inventoryClass(item.inventory)}`}>
                    {item.inventory}
                  </td>
                  <td className={styles.dateCell}>{formatDate(item.lastModifiedDateTime)}</td>
                </tr>
              ))}
              {sorted.length === 0 && !isLoading && (
                <tr>
                  <td colSpan={6} style={{ textAlign: 'center', padding: '32px', color: 'var(--color-text-secondary)' }}>
                    Ingen varer matcher søket.
                  </td>
                </tr>
              )}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Steg 3: Verifiser TypeScript-kompilering**

```bash
npm run build
```

Forventet: Ingen feil.

- [ ] **Steg 4: Commit**

```bash
git add src/pages/Lager/
git commit -m "feat: legg til Lager-side med tabell, søk og filtrering"
```

---

## Task 8: Ruting og navigasjon

**Files:**
- Modifiser: `src/App.tsx`
- Modifiser: `src/components/Layout/LayoutV2.tsx`

- [ ] **Steg 1: Legg til import og route i `src/App.tsx`**

Legg til import etter eksisterende imports (f.eks. etter `Team`-importen):

```tsx
import { Lager } from './pages/Lager/Lager';
```

Legg til route inne i `<Route path="/" element={<Layout />}>` – plasser den mellom `/team` og `/todos`:

```tsx
<Route path="lager" element={<Lager />} />
```

- [ ] **Steg 2: Legg til nav-innslag i `src/components/Layout/LayoutV2.tsx`**

Legg til `Package` i eksisterende lucide-react import:

```tsx
import {
  LayoutDashboard, FileText, Kanban, CheckSquare, Settings,
  AlertTriangle, Sparkles, Calendar, TrendingUp, PanelLeftOpen,
  PanelLeftClose, Wand2, Users, Package,
} from 'lucide-react';
```

Legg til i `navItems`-arrayen mellom `Team` og `Mine oppgaver`:

```tsx
{ path: '/lager', icon: Package, label: 'Lager' },
```

- [ ] **Steg 3: Start appen og verifiser navigasjon**

```bash
npm start
```

Åpne `http://localhost:5173`. Verifiser:
- «Lager» vises i sidebar-menyen med Package-ikon
- Klikk fører til `/lager`
- Siden laster og viser spinner mens data hentes
- Data fra BC vises i tabellen
- Søk, gruppe-filter og toggle fungerer
- Oppdater-knapp henter nye data

- [ ] **Steg 4: Bygg for produksjon og verifiser**

```bash
npm run build
```

Forventet: Bygger uten feil.

- [ ] **Steg 5: Commit**

```bash
git add src/App.tsx src/components/Layout/LayoutV2.tsx
git commit -m "feat: legg til /lager-route og nav-innslag for lagersiden"
```

---

## Verifisering av «Definisjon av ferdig»

- [ ] Lagerdata kan hentes fra BC med spesifiserte felter – verifiser i konsoll
- [ ] Data vises på menyvalg «Lager» i appen
- [ ] Token-caching fungerer – sjekk konsollutput: første kall = «Henter nytt token», andre kall = «Cache-treff»
- [ ] Paginering: sjekk konsollutput for antall sider ved > 1000 items
- [ ] Feilhåndtering fungerer – test ved å sette feil `BC_CLIENT_SECRET` i `.env`
- [ ] `BC_CLIENT_SECRET` er ikke i git – kjør `git log --all -- .env` og verifiser ingen treff
- [ ] `.env.example` er committed uten secret
